// ส่วนที่ 2

import * as React from "react"
import Layout from "../components/layout"

export default function MemberInfoPage (props) {

  const data = props.pageContext.member // เข้าถึงข้อมูลจากตัวแปร data

  return (
    <Layout>
      {/* เขียนโค้ดโลดดดด */}
    </Layout>
  )
}