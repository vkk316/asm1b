import * as React from "react"
import Layout from "../components/layout"

export default function SubjectDetailPage (props) {

  const data = props.pageContext.subject // เข้าถึงข้อมูลจากตัวแปร data

  return (
    <Layout>
      {/* เขียนโค้ดโลดดดด */}
    </Layout>
  )
}